App.jsx

1)	if scroll active for page changing, there must be 
	icon or blinking symbol

2) Loading screen at contentBox with gif/loading animation


Pages 
1) Remove flex from ContentBox
2) Add flex to each page wrapper, if needed


Uprise

1) a lot of code 
2) one class for all -> uprise--[up,down,left,right]--l[length]--t[time]--d[delay]--as[for autoshow]--ah[for autohide]--p[for show/hide by parent event]


Scroll

1) if scrollbottom bounce timer <= changePageHeight server answer  => autowaiting of changeBottom (loop : refreshParameters -> scrollBotomEvent -> bad server answer with no change of scrollBottomPlace -> refreshParameters...)

2) App scroll enable/disable
App elem scroll enable/disable

CustomScroll.jsx bounceScrollBotomEvent time < bad server answer time